{% extends 'base.html.twig' %}

{% block title %}Form theme{% endblock %}

{% block body %}
    <h1>Nouveau theme</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}
